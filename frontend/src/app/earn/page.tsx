import { EarnInterface } from "@/components/earn/EarnInterface";

export const metadata = {
  title: "Earn | sBTC Lending Pool",
  description: "Deposit sBTC to earn interest and dual stacking rewards.",
};

export default function EarnPage() {
  return <EarnInterface />;
}
